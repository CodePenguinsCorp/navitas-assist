spring:
  datasource:
    url: jdbc:mysql://127.0.0.1:3306/navitas_assist?createDatabaseIfNotExist=true&useSSL=false&allowPublicKeyRetrieval=true&serverTimezone=UTC
    username: navitas_app
    password: navitas123

server:
  port: ${SERVER_PORT:8080}

navitas:
  security:
    seed-admin:
      username: ${APP_ADMIN_USERNAME:admin}
      password: ${APP_ADMIN_PASSWORD:admin123}
      full-name: ${APP_ADMIN_FULL_NAME:Administrador}
